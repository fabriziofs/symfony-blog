{% extends 'base.html.twig' %}

{% block body %}
    <div class="grid gap-2 place-items-center py-8 px-4">
        <h1 class="text-2xl">👨‍💻DebVlogs👩‍💻</h1>
        <h2 class="text-sm">Simple lectures to learn useful thing for your tech career</h2>
    </div>
    <section class="grid gap-4 [grid-template-columns:_repeat(auto-fill,_minmax(16rem,_1fr))]">
        {% for post in posts %}
            {{ include('post/_partials/_card.html.twig') }}
        {% endfor %}
    </section>
{% endblock %}
